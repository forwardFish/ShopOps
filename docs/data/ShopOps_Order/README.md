# ShopOps Order Runtime Directory

This directory is intentionally tracked so a fresh checkout has the expected
runtime path for Tmall order Excel exports.

Downloaded order files are generated at runtime and should not be committed.
